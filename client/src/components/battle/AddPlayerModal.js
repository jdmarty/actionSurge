import React, { useState } from "react";
import Modal from "react-modal";

function AddPlayerModal(props) {
  return <Modal {...props} >
    <h1>Click to add a character</h1>
  </Modal>
}

export default AddPlayerModal